[package]
name = "sentinel"
version = "0.1.0"
edition = "2021"
description = "A brute-force / suspicious-login detector for SSH auth logs"
authors = ["Elliot"]
license = "MIT"

[dependencies]
regex = "1"

[profile.release]
opt-level = 3
lto = true
